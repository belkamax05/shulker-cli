local debug = false
local verbose = false
local trace = false
local watch = false

--? Uncomment the following lines to enable debugging, verbose output, or tracing
debug = true
-- verbose = true
trace = true

return {
    debug = debug,
    verbose = verbose,
    trace = trace,
    watch = watch,
}
